import styled, { css } from 'styled-components';

export const CommandHistoryMainContainer = styled.div`

`;

export const CommandHistoryContainer = styled.div`
    font-size:1.2rem;
`;

export const CommandContainer = styled.div`
    display:flex;
    
`;

